globalVariables(c("tidyverse", "mdsr2exercises"))

#' Package dependencies for mdsr2exercises package.
#'
#' @export
setup <- function() {
  library(tidyverse)
  library(mdsr)
  library(mdsr2exercises)
}
